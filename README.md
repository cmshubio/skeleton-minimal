# CmsHub Minimal Skeleton

Install [cmshub client](https://github.com/cmshubio/homebrew-cmshub), next init the project:

```
$ cmshub init --skeleton=minimal
```

Start the project locally:

```
$ cmshub serve
```
